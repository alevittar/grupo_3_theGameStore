const express = require('express');
const app = express();

app.get('/serie/:id', (req, res) => {
    let id = req.params.id;

    let serie = series.find(s => s.id == id);

    if (serie) {
        res.send(serie);
    }
    
});